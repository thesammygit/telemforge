# Fixtures

This directory is the home for human-readable fixture data.

Stage 02 should define telemetry contracts and examples under `fixtures/telemetry/`. Fixtures should stay small, inspectable, and suitable for contract tests. Do not commit generated bulk telemetry dumps unless a later stage explicitly approves that artifact.
